# Заготовка: Якутск (WMO 24959)

Папка наполнится при `run_fast_extract.py` (по умолчанию весь `default_region` из `stations_catalog.yaml`).
Одна станция: `py -3 run_fast_extract.py --station yakutsk`
